# CLAUDE.md

This file provides guidance for AI assistants (Claude and others) working in this repository.

## Repository Status

This repository is currently in its initial state — only a `.gitattributes` file has been committed. No source code, dependencies, or configuration exists yet.

Update this file as the project grows to reflect the actual structure, conventions, and workflows.

---

## Repository Overview

- **Repository**: yoel-projects
- **Remote**: http://local_proxy@127.0.0.1:22054/git/yoelgrosman-cmd/yoel-projects
- **Default branch**: master

---

## Git Conventions

### Branch Naming
- Feature branches: `feature/<short-description>`
- Bug fixes: `fix/<short-description>`
- Claude-created branches: `claude/<task-id>` (auto-generated by Claude Code)

### Commit Messages
- Use the imperative mood: "Add feature" not "Added feature"
- Keep the subject line under 72 characters
- Reference issue numbers when applicable: `Fix login bug (#42)`

### Workflow
1. Branch off `master` for all changes
2. Make focused, atomic commits
3. Push the branch and open a pull request
4. Merge after review

---

## Development Setup

> No project-specific setup exists yet. Fill this section in once a tech stack is chosen.

Typical setup steps will go here, for example:
```bash
# Install dependencies
npm install        # Node.js
pip install -r requirements.txt  # Python

# Run the development server
npm run dev

# Run tests
npm test
```

---

## Project Structure

> To be filled in once source code is added.

A typical layout might look like:
```
yoel-projects/
├── src/            # Application source code
├── tests/          # Test files
├── docs/           # Documentation
├── .gitattributes  # Git line-ending normalization
└── CLAUDE.md       # This file
```

---

## Testing

> No tests exist yet. Document the testing framework and commands here once added.

---

## Code Style & Linting

> No linting or formatting configuration exists yet. Document tools and commands here once added.

---

## CI/CD

> No CI/CD pipelines exist yet. Document pipeline details here once added.

---

## Notes for AI Assistants

- **This repo is empty.** Do not assume any tech stack, framework, or conventions beyond what is documented here.
- Before making changes, check this file and any README for up-to-date context.
- When adding the first meaningful code, update the relevant sections of this file to reflect the chosen stack and conventions.
- Follow the Git conventions above for all branches and commits.
- Prefer small, focused commits over large sweeping changes.
- Do not push directly to `master`; always use a feature branch and pull request.
